<Window x:Class="PWSV.Client.Views.Categories.CategoryEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:PWSV.Client.Converters"
        Title="Категорія" Width="460" Height="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Категорія" Style="{StaticResource HeadingTextStyle}" />
                <TextBlock Text="Заповніть дані категорії"
                           Style="{StaticResource SubtitleTextStyle}" />
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource ErrorBannerStyle}"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource ErrorHoverBrush}"
                           TextWrapping="Wrap" />
            </Border>

            <StackPanel Grid.Row="2">
                <TextBlock Text="Назва" Style="{StaticResource LabelTextStyle}" />
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" TabIndex="0" />

                <TextBlock Text="Тип" Style="{StaticResource LabelTextStyle}" />
                <TextBox Text="{Binding Kind}" IsReadOnly="True" TabIndex="1" />

                <TextBlock Text="Батьківська категорія (опціонально)"
                           Style="{StaticResource LabelTextStyle}" />
                <ComboBox ItemsSource="{Binding Parents}"
                          SelectedItem="{Binding SelectedParent}"
                          DisplayMemberPath="Name" TabIndex="2" />
            </StackPanel>

            <Border Grid.Row="3" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,1,0,0" Margin="0,16,0,0" Padding="0,16,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Скасувати" Click="OnCancelClick"
                            Style="{StaticResource SecondaryButtonStyle}" TabIndex="4" />
                    <Button Content="Зберегти" Command="{Binding SaveCommand}"
                            IsDefault="True" TabIndex="3" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
